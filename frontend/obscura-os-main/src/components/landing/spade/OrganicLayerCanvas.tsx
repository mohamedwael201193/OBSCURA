import OrganicFigureCard from "./OrganicFigureCard";

/** Light card variant — used in the SDK / builders section */
export default function OrganicLayerCanvas() {
  return <OrganicFigureCard variant="light" />;
}
